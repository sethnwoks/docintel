# OpenAI API key (required for embeddings + LLM)
OPENAI_API_KEY=

# JWT secret (min 32 chars, required for auth)
JWT_SECRET_KEY=

# Optional: set to 'production' for HTTPS-only cookies
# ENV=production
